apply plugin: "war"

bootWar {
    mainClass = "com.ontarget.testing.TestingjhipsterprojectApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
